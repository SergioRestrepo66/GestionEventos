import React from 'react';
import { Typography } from '@mui/material';

const Inicio = () => {
    return (
        <div>
            <Typography variant="h4">Bienvenido a la Aplicación Gestion de Eventos</Typography>
        </div>
    );
}

export default Inicio;